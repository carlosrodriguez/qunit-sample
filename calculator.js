var CALC;

CALC = CALC || {};

CALC.calculator = (function(my) {
	// Set defaults
	var urgency = 0.5,
		complexity = 0.5;
	// etc


	my.baseSimple = function (opts) { // set opts as an object
		return true; // set formula here
	};

	return my;
})(CALC);
